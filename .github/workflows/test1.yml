_schema-version: "3.3"
ID: employee-details
version: 1.0.0

parameters:
  enable-parallel-deployments: true

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm install
        - npx cds build --production

modules:

  - name: employee-details-srv
    type: nodejs
    path: gen/srv

    parameters:
      memory: 512M
      disk-quota: 512M

    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}

    requires:
      - name: employee-details-db
      - name: employee-details-xsuaa
      - name: employee-details-destination

    build-parameters:
      builder: npm-ci

  - name: employee-details-db-deployer
    type: hdb

    path: gen/db

    requires:
      - name: employee-details-db

resources:

  - name: employee-details-db
    type: com.sap.xs.hdi-container

    parameters:
      service: hana
      service-plan: hdi-shared

  - name: employee-details-xsuaa
    type: org.cloudfoundry.managed-service

    parameters:
      service: xsuaa
      service-plan: application
      path: xs-security.json

  - name: employee-details-destination
    type: org.cloudfoundry.managed-service

    parameters:
      service: destination
      service-plan: lite
